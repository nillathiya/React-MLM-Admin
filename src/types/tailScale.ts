export interface TailScale {
  _id: string;
  hostname: string;
  addresses: [string];

  os: string;
  name: string;
  status: number;
  lastSeen: string;
  createdAt: string;
  updatedAt: string;
}
